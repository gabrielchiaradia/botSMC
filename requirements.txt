# ══════════════════════════════════════════════
#  SMC Futures Bot — Dependencias
# ══════════════════════════════════════════════

# Core
python-binance>=1.0.19
pandas>=2.0.0
numpy>=1.24.0
requests

# Variables de entorno
python-dotenv>=1.0.0

# Display en consola
colorama>=0.4.6
tabulate>=0.9.0

# SMC indicators (opcional, el bot tiene fallback manual)
# smartmoneyconcepts>=0.0.3

# Caché de datos
pyarrow>=14.0.0      # Para .parquet (más rápido que CSV)

# Tests
pytest>=7.4.0
pytest-cov>=4.1.0

# Desarrollo (opcional)
# black>=23.0.0
# mypy>=1.5.0
# ipykernel>=6.25.0

# Fase 3
websocket-client>=1.6.0  # WebSocket stream en tiempo real
